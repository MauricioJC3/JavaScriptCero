// 7. Multiplicación con función
//    - Crea una función `multiplicar(a, b)`
//     que devuelva el resultado de `a * b`.

const funcionMultiplicar = (a, b) => {
    console.log(a * b);
}

funcionMultiplicar(2, 3);